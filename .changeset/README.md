# Changelog

Changesets live in this directory until a release. The release workflow publishes `transcripts-mcp` to public npm from git tags (`v*`), not from `changeset publish`.

See [the changesets docs](https://github.com/changesets/changesets).
