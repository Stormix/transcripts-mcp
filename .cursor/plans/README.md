# Implementation Plans

Generated by the improve skill on 2026-09-06 against commit `eba26f3`. Execute in the order below unless dependencies say otherwise. Each executor must read its plan fully, honor its STOP conditions, run every verification gate, and update its status row.

## Execution order and status

| Plan | Title                                                    | Priority | Effort  | Depends on    | Status |
| ---- | -------------------------------------------------------- | -------- | ------- | ------------- | ------ |
| 001  | Enforce canonical transcript-file boundaries             | P1       | S       | —             | DONE   |
| 002  | Restrict native grep to adapter session globs            | P1       | S       | 001           | DONE   |
| 003  | Make indexed date filters consistent and truthful        | P1       | M       | —             | DONE   |
| 004  | Track complete semantic-index readiness                  | P1       | M       | —             | DONE   |
| 005  | Scope persisted search results to active adapter roots   | P1       | M       | 001           | DONE   |
| 006  | Bound fallback regex and transcript-read resources       | P1       | M       | 001           | DONE   |
| 007  | Establish one checked toolchain version source           | P1       | S       | —             | DONE   |
| 008  | Synchronize all release-facing versions                  | P2       | S       | 007           | DONE   |
| 009  | Stop rescanning JSONL prefixes during grep normalization | P2       | M       | 002, 006      | DONE   |
| 010  | Page and batch semantic corpus construction              | P2       | M       | 004           | DONE   |
| 011  | Bound filtered vector retrieval                          | P2       | M–L     | 003, 004      | DONE   |
| 012  | Smoke-test built release artifacts                       | P1       | M       | 007           | DONE   |
| 013  | Establish one checked MCP tool-contract source           | P2       | M       | 003, 006      | DONE   |
| 014  | Design transcript windows anchored to search hits        | P2       | M spike | 006           | TODO   |
| 015  | Design explicit, safe index freshness                    | P2       | M spike | 004, 005, 010 | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- 002 follows 001 so native grep uses the same definition of a valid transcript file as direct retrieval.
- 005 follows 001 because persisted results must use canonical root and session-path identities.
- 008 and 012 follow 007 so release automation and its tests use one pinned runtime matrix.
- 009 follows 002 and 006 so optimization preserves the corrected corpus and resource budgets.
- 010 and 011 follow 004 because batching or retrieval changes must not preserve the current partial-readiness bug.
- 011 also follows 003 because pushing filters into SQL requires canonical time-filter semantics.
- 013 follows 003 and 006 so generated metadata documents the corrected validation and limits.
- 014 follows 006 so its API design builds on bounded session reading.
- 015 follows 004, 005, and 010 so freshness cannot expose stale roots, claim partial semantic readiness, or trigger an unbounded refresh path.

## Findings considered and rejected

- `adm-zip@0.5.18` and `sharp@0.34.5` advisories: not promoted because the audited runtime accepts transcript text, not caller-controlled ZIP/image inputs; track upstream fixes and reassess if model artifacts or image APIs become caller-controlled.
- SQL injection in FTS: rejected because dynamic clauses are code-owned and all user values are bound parameters.
- Splitting `fts.ts` or `adapter.ts` solely by file size: rejected; both remain cohesive and have focused tests.
- A broad dependency migration: rejected; catalog pinning, minimum release age, and Dependabot are already present, with no evidenced EOL blocker.
- A long-lived shared SQLite handle: rejected without measurements showing reopen cost dominates; it adds concurrency and lifecycle risk.
- Deep frontend test expansion: deferred because no concrete website behavior defect was reproduced; release-artifact smoke coverage has higher leverage.

## Audit limits

Generated files, `node_modules`, `dist`, the untracked `.deepsec/` directory, dependency internals, downloaded semantic models, live deployment behavior, and non-Windows release binaries were not deeply audited.
